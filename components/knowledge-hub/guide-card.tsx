/* eslint-disable @next/next/no-img-element -- Supabase bucket host is deployment-specific. */
import Link from "next/link";
import type { KnowledgeHubItem } from "@/lib/types/database";
const date = (v: string | null) => v ? new Intl.DateTimeFormat("en-IN", { dateStyle: "medium" }).format(new Date(v)) : ""; const size = (n: number) => `${(n / 1024 / 1024).toFixed(1)} MB`;
export function GuideCard({ guide }: { guide: KnowledgeHubItem }) { return <article className="flex min-w-0 flex-col overflow-hidden rounded-2xl border border-[#E5E7EB] bg-white shadow-sm"><div className="aspect-[16/9] bg-[#E5E7EB]">{guide.thumbnail_url ? <img alt="" className="h-full w-full object-cover" src={guide.thumbnail_url}/> : <div className="flex h-full items-center justify-center bg-gradient-to-br from-[#DBEAFE] to-[#E5E7EB] font-bold text-[#1D4ED8]">HypeBuzz PDF Guide</div>}</div><div className="flex flex-1 flex-col p-5"><p className="text-xs font-bold uppercase tracking-wider text-[#2563EB]">{guide.category}</p><h2 className="mt-2 text-xl font-bold leading-snug"><Link className="hover:text-[#1D4ED8]" href={`/knowledge-hub/${guide.slug}`}>{guide.title}</Link></h2><p className="mt-3 line-clamp-3 text-sm leading-6 text-[#6B7280]">{guide.description}</p><p className="mt-4 text-xs text-[#6B7280]">{date(guide.published_at)} · PDF, {size(guide.pdf_size_bytes)}</p><div className="mt-5 grid grid-cols-2 gap-2"><a className="inline-flex min-h-11 items-center justify-center rounded-[10px] bg-[#2563EB] px-3 text-sm font-semibold text-white" href={guide.pdf_url} rel="noopener noreferrer" target="_blank">View PDF</a><a className="inline-flex min-h-11 items-center justify-center rounded-[10px] border border-[#D1D5DB] px-3 text-sm font-semibold" download href={guide.pdf_url}>Download PDF</a></div></div></article>; }
